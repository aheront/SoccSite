- @res.audios.each do |audio|
  %br
  = audio.sound_file_name
  = link_to '', user_audio_path(@res,audio),method: :delete, class: 'glyphicon glyphicon-remove'
  %br
  = audio_tag(audio.sound.url,  controls: true,style:'width:700px')
  %br